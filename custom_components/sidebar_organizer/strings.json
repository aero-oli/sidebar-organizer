{
  "title": "Sidebar Organizer",
  "config": {
    "step": {
      "user": {
        "title": "Sidebar Organizer",
        "description": "Configure the shared YAML file and private per-user profile storage used by Sidebar Organizer.",
        "data": {
          "config_path": "Config path",
          "profiles_path": "User profiles directory",
          "allow_write": "Allow writing from the UI",
          "allow_user_write": "Allow users to edit their own profile",
          "create_if_missing": "Create the file if missing"
        }
      }
    },
    "error": {
      "invalid_path": "The config and profiles paths must resolve inside the Home Assistant config directory."
    },
    "abort": {
      "already_configured": "Sidebar Organizer is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Sidebar Organizer options",
        "description": "Update the shared YAML and per-user profile settings.",
        "data": {
          "config_path": "Config path",
          "profiles_path": "User profiles directory",
          "allow_write": "Allow writing from the UI",
          "allow_user_write": "Allow users to edit their own profile",
          "create_if_missing": "Create the file if missing"
        }
      }
    },
    "error": {
      "invalid_path": "The config and profiles paths must resolve inside the Home Assistant config directory."
    }
  }
}
